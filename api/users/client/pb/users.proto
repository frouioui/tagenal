syntax = "proto3";

option go_package = "github.com/frouioui/tagenal/users/client/pb";

package pb;

message UserRequest {
    uint64 a = 1;
    uint64 b = 2;
}

message UserResponse {
    uint64 result = 1;
}

service UserService {
    rpc Compute (UserRequest) returns (UserResponse);
}